version: '2'
services:
  proharvest-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/proharvest/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=proharvest
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
